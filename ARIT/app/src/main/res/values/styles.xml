<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="actionBarStyle">@style/AppThemeActionBarLogo</item>
        <item name="android:textViewStyle">@style/FontText</item>
        <item name="android:buttonStyle">@style/FontButton</item>
    </style>

    <style name="AppThemeActionBarLogo" parent="@style/Widget.AppCompat.Light.ActionBar">
        <item name="background">@color/black</item>
        <!--<item name="logo">@drawable/logo</item>
        <item name="displayOptions">useLogo|showHome</item>-->
    </style>

    <style name="FontText" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/silgothic</item>
    </style>

    <style name="FontButton" parent="android:Widget.Button">
        <item name="android:fontFamily">@font/silgothic</item>
    </style>

</resources>
